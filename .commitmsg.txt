Fix generation timeout + add config import (v1.0.1)

- ServiceLocator: bump OkHttp read/write timeouts to 10 minutes and disable
  overall callTimeout. Azure /edits with reference images routinely takes
  3+ minutes, which the previous 180s read timeout was cutting off and
  surfacing as "Generation failed".
- ServiceLocator: bootstrap secure config from
  filesDir/imagen-bootstrap-config.json if present (debug helper).
- FirstRunSetup: add "Import from config.json" button using SAF; accepts
  endpoint/apiKey/deployment/apiVersion plus common aliases.
- AppViewModel: log generation failures via Log.e("Imagen", ...) so adb
  logcat surfaces the underlying cause.
- Bump versionCode 1 -> 2, versionName 1.0.0 -> 1.0.1.
